# macOS / OS noise
.DS_Store
Thumbs.db
*.swp

# Secrets / credentials (never commit)
.env
.env.*
*.pem
*.key
*.pfx
*.p12
secrets.*
**/local.settings.json
.azure/

# Build / dependency output
node_modules/
dist/
build/
.venv/
__pycache__/
*.pyc

# Editor / tooling
.idea/
.vscode/
*.log

# Data (never commit real/exported data)
*.csv
*.parquet
/data/
/exports/

# Local-only real-name presentation build (NEVER commit real names)
Board pitch/_present/
Board pitch/build_realnames.sh

# Local-only decision memo: contains real names and dollar figures. NEVER commit.
# Anonymize the source before any of these may be tracked.
Board pitch/Owned_Brands_Decision_Memo*.docx
Board pitch/Owned_Brands_Decision_Memo*.pdf
Board pitch/build_memo.js

# QA scratch from doc rendering (regenerated on demand)
Board pitch/*.jpg
Board pitch/tour-*.png
Board pitch/raw/
Board pitch/unpacked/
Board pitch/unpacked*/
